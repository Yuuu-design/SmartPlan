# SHENGHU SmartPlan 答辩演示脚本（5 分钟）

> 演示前准备：后端 `uvicorn src.api.v1.schedule:app` 已启动（8000 端口）、前端 `npm run dev`（5173 端口）。浏览器打开 `http://localhost:5173/`，等甘特图加载完成（约 2 秒）。

---

## 开场（0:00–0:30）

**屏幕**：停留在已加载完成的排产驾驶舱（甘特图 + 顶部 KPI）。

**台词**：

> 从订单到机台，我们要解决的核心问题是：把一批钢丝绳订单，拆成**拉丝 → 捻股 → 合绳**三道工序，在 100 多台设备、规格适配、工序先后、换型时间这些硬约束下，排出一个**交期最靠前**的计划。
>
> 难点就三点：**交期紧、设备忙闲不均、插单打乱已有计划**。我们的方案，就是用运筹优化做计算、用 AI 做解释，实现"AI 计算 + 人工决策"的闭环。

---

## 第一幕：自动排产（0:30–1:20）

**操作**：鼠标指向顶部 KPI 卡片，再指向甘特图。

**台词**：

> 这是一次真实的 302 单全量排产结果。看顶部四个指标——**准时交付率 98%+**，这是我们的第一优先级；**活跃设备利用率 70%**，合绳工序 80% 是瓶颈，上游拉丝、捻股按需拉动。
>
> 中间是自研的 SVG 甘特图，**设备视图**看每台机台的负荷，**订单视图**看单个订单的三工序流转。这里可以切换、缩放、拖拽调整。

**操作**：点一下"订单视图"，展示一个订单的拉丝→捻股→合绳三段颜色不同的块 + 橙色依赖连线。

---

## 第二幕：可解释性（1:20–1:50）

**操作（两种方式，任选其一，推荐 B）**：

- 方式 A：确认在**设备视图**（顶部「设备视图」按钮高亮），鼠标点甘特图上任意一个任务块；
- 方式 B：看右侧栏，向下滚到「**风险与异常**」卡片，点其中一条风险订单的「**定位 →**」——甘特图会自动平滑滚动到该任务并选中。

**屏幕变化**：右侧栏顶部弹出「任务详情」卡片——工序彩色标签、准时/延期状态、设备、时间窗口、规格数量，下方「**决策依据（规则可追溯）**」列出 **R1 规格匹配**（设备规格区间覆盖任务规格）、**R3 换型**（免换型或 N 分钟换型）、**R5/R6 工序衔接** 等条目。

**台词**：

> 我们的排产不是黑盒。每一个任务都能**指回规则依据**——为什么排在这台设备、为什么是这个时间，都能追溯到 R1 规格匹配、R3 换型优化、R6 工序衔接这些工业规则；4 小时内已下发的任务还受 R8 冻结窗口保护。这是计划员敢用的前提。

> 提示：讲完点卡片右上角「×」或甘特图空白处关闭；卡片底部的「锁定该任务」是第四幕要用的入口，这里先不用点。

---

## 第三幕：异常注入 + 多方案（1:50–3:30）

**操作**：点右下角 ⚡ 悬浮按钮 → 选「8301 合绳机宕机 6 小时」。

**台词**：

> 现在合绳瓶颈设备 8301 突然宕机 6 小时。系统不是推倒重来，而是做**最小扰动重排**，并且给出三套可比较的方案。

**操作**：等三方案对比表出现，逐一点击 A / B / C，展示甘特图的 Diff 虚线（偏离基线的任务显示原位置）。

**台词**：

> **方案 A 保交期**、**方案 B 少扰动**、**方案 C 高效率**。注意方案 B——扰动只有几十分钟、调整的任务最少，维持车间秩序；方案 C 为了效率大调，扰动上千分钟。系统不替计划员做决定，而是量化每个选择的后果，**由人确认**。

**操作**：点方案 B，然后点「关闭」回到基线。

---

## 第四幕：锁单 + 最小扰动（3:30–4:10）

**台词**：

> 再补充一个人工干预闭环。计划员可以**锁定**关键任务——锁定的任务在重排时保持原位，是硬约束。

**操作**：
1. 点一个**非 8301 设备**上的任务块（建议选 8246 合绳机或捻股机上、块较宽的任务，便于观众看清），右侧栏弹出任务详情卡片（块上方也有小工具条）；
2. 点卡片底部的「**锁定该任务**」（或工具条上的「锁定任务」）——任务块右上角出现 🔒 图标，按钮变为蓝色「解除锁定」，此时该任务无法再拖拽；
3. 点右下角 ⚡ →「**8301 合绳机宕机 6 小时**」，等三方案出现；
4. 点方案 B，向观众指出：**锁定的任务设备和时间纹丝不动**（Diff 图上没有虚线偏移），其余任务在约束内最小扰动。
5. 演示完点「关闭」回基线；如要取消锁单，点该任务块 →「解除锁定」。

> 提示：锁的是任务**实例**（某订单的某道工序），不是整台设备；不要锁 8301 上的任务——故障设备上的锁会被系统安全解除（否则模型无解）。

---

## 第五幕：AI Copilot（4:10–4:40）

**操作**：点右下角「AI 排产助手」，点快捷指令「模拟 8301 宕机」，或用自然语言输入"8301 坏了 6 小时怎么办"。

**台词**：

> 最后是 AI 对话。计划员用自然语言问"8301 坏了怎么办"，AI 理解意图、调用排产工具、把结果润色成计划员能听懂的话，并附上方案卡片。**注意：AI 不做数学计算，计算是 OR-Tools 的，AI 只做意图识别和解释**——这是算控分离的架构原则。

---

## 收尾（4:40–5:00）

**台词**：

> 总结一下闭环：**自动排产 → 风险发现 → 原因解释 → 异常重排 → 多方案推演 → 人工确认**。
>
> 这套系统的价值不在于"替你排"，而在于把排产、解释、预警、重排变成一个**计划员敢用、能控**的决策过程。计划可以自动生成，但真正的智能，体现在计划出问题之后。

---

## 备选话术（如果时间被压缩）

- 砍掉第五幕（AI Copilot），4 分钟内也能完整讲清核心价值。
- 如果评委对某个点特别感兴趣（比如稀疏图、可解释性），可以临时展开，但控制在一分钟内。
